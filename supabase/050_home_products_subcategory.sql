-- Подкатегории продуктов внутри категории (напр. "Еда" -> мясо/вода/фрукты/
-- сладкое/овощи) — пользователь задаёт их сам произвольным текстом, без
-- фиксированного списка.

ALTER TABLE home_products ADD COLUMN IF NOT EXISTS subcategory text;
